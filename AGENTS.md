# AGENTS.md

## Project Overview

This project is a synthesizer application comprising a synthesizer kernel (`src/kernel`), a public API layer (`src/api`), and Slint-based UI components (`src/slint-ui`).

- Never Comment On Github
- Never Edit README.md