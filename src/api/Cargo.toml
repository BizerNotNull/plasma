[package]
name = "plasma-api"
version = "0.1.0"
edition = "2024"
description = "Shared synth controls and system audio for Plasma"
license = "Apache-2.0"

[lib]
path = "lib.rs"

[dependencies]
cpal = "0.15.3"
plasma-kernel = { path = "../kernel" }
